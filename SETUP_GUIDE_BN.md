# ফেসবুক পেজ স্মার্ট এআই অটোমেশন - সম্পূর্ণ সেটআপ গাইড (বাংলায়)

এই গাইডের সাহায্যে আপনি খুব সহজে আপনার ফেসবুক পেজ, টেলিগ্রাম বট এবং জেমিনি এআই কনফিগার করে সিস্টেমটিকে ২৪/৭ চালু করতে পারবেন। আপনার কম্পিউটার বা ফোন বন্ধ থাকলেও এটি সার্বক্ষণিক কাজ করবে।

---

## ধাপ ১: Google Gemini API Key নেওয়া (সম্পূর্ণ ফ্রি)

1. ব্রাউজারে যান: [Google AI Studio](https://aistudio.google.com/)
2. আপনার গুগল (Gmail) অ্যাকাউন্ট দিয়ে সাইন ইন করুন।
3. বাম পাশের মেনু থেকে **"Get API key"** বাটনে ক্লিক করুন।
4. **"Create API key"**-তে চাপ দিন এবং একটি নতুন প্রজেক্ট সিলেক্ট করে কী তৈরি করুন।
5. এপিআই কী-টি কপি করে রাখুন।

---

## ধাপ ২: টেলিগ্রাম বট (Telegram Bot) ও চ্যাট আইডি তৈরি

### ক. টেলিগ্রাম বট তৈরি
1. আপনার টেলিগ্রাম অ্যাপে সার্চ করুন: `@BotFather` (নীল ভেরিফায়েড টিক দেওয়া)।
2. চ্যাটে গিয়ে লিখুন: `/newbot`
3. বটের একটি নাম দিন (যেমন: `My Page Auto Bot`)।
4. এরপর বটের একটি ইউনিক ইউজারনেম দিন যার শেষে `bot` থাকতে হবে (যেমন: `mypage_ai_bot`)।
5. বটফাদার আপনাকে একটি **HTTP API Token** দেবে (যেমন: `7123456789:AAHxxxxxxxxxxxxxxxxxxxx`)। এটি কপি করে রাখুন।

### খ. আপনার Telegram Chat ID বের করা
1. টেলিগ্রামে সার্চ করুন: `@userinfobot`
2. বটটিতে গিয়ে `/start` চাপুন।
3. বটটি আপনাকে আপনার `Id` (যেমন: `1234567890`) দেখাবে। এটিই আপনার **Admin Chat ID**।

---

## ধাপ ৩: ফেসবুক মেটা ডেভেলপার অ্যাপ ও পেজ টোকেন সেটআপ

### ক. মেটা ডেভেলপার অ্যাকাউন্ট তৈরি
1. ব্রাউজারে যান: [Meta for Developers](https://developers.facebook.com/)
2. আপনার ফেসবুক অ্যাকাউন্ট দিয়ে লগইন করুন এবং ডেভেলপার হিসেবে রেজিস্টার করুন।

### খ. নতুন অ্যাপ তৈরি (Create App)
1. উপরের মেনু থেকে **"My Apps"**-এ যান এবং **"Create App"** বাটনে ক্লিক করুন।
2. Use Case হিসেবে **"Other"** সিলেক্ট করুন এবং Next চাপুন।
3. App Type হিসেবে **"Business"** সিলেক্ট করুন।
4. App-এর একটি নাম দিন (যেমন: `Page AI Automation`) এবং আপনার ইমেইল দিয়ে **"Create app"** সম্পন্ন করুন।

### গ. পেজ অ্যাক্সেস টোকেন (Page Access Token) তৈরি
1. মেনু থেকে যান: [Graph API Explorer](https://developers.facebook.com/tools/explorer/)
2. ডানপাশে আপনার তৈরি করা App সিলেক্ট করুন।
3. **User or Page** ড্রপডাউন থেকে **"Get Page Access Token"** সিলেক্ট করুন এবং আপনার ফেসবুক পেজটি সিলেক্ট করুন।
4. **Add a permission** বাটনে ক্লিক করে নিচের পারমিশনগুলো যুক্ত করুন:
   - `pages_show_list`
   - `pages_read_engagement`
   - `pages_manage_posts` (পোস্ট পাবলিশ করার জন্য)
   - `pages_manage_metadata`
   - `pages_messaging` (মেসেঞ্জার মেসেজ রিসিভ ও পাঠানোর জন্য)
5. **"Generate Access Token"** বাটনে ক্লিক করুন।
6. **টোকেনটিকে লং-লিভড (Long-lived Token) করার নিয়ম:**
   - [Access Token Debugger](https://developers.facebook.com/tools/debug/accesstoken/) টুলে যান।
   - প্রাপ্ত টোকেনটি পেস্ট করে **Debug** চাপুন।
   - নিচে **"Extend Access Token"** বাটনে ক্লিক করলে আপনি একটি পার্মানেন্ট লং-লিভড টোকেন পেয়ে যাবেন, যা মেয়াদ শেষ হবে না।

### ঘ. ফেসবুক পেজ আইডি (Page ID) সংগ্রহ
1. আপনার ফেসবুক পেজে ঢুকুন।
2. **About** ট্যাবে যান অথবা পেজের সেটিংসে যান।
3. সেখানে **Page ID** (একটি সংখ্যা, যেমন: `100085492817263`) দেখতে পাবেন।

---

## ধাপ ৪: প্রোজেক্ট কনফিগারেশন (`.env` ফাইল)

1. প্রজেক্ট ফোল্ডারে থাকা `.env.example` ফাইলটিকে কপি করে নাম দিন `.env`।
2. `.env` ফাইলে আপনার সংগৃহীত ডেটাগুলো বসিয়ে দিন:

```env
GEMINI_API_KEY=AIzaSyDxxxxxxxxxxxxxxxxxxxxxxxx
TELEGRAM_BOT_TOKEN=7123456789:AAHxxxxxxxxxxxxxxxxxxxx
TELEGRAM_ADMIN_CHAT_ID=1234567890

FB_PAGE_ID=100085492817263
FB_PAGE_ACCESS_TOKEN=EAAG...আপনার_লং_লিভড_পেজ_টোকেন...
FB_VERIFY_TOKEN=my_secure_fb_webhook_token_2026

# যে ইউটিউব চ্যানেল থেকে স্বয়ংক্রিয় পোস্ট বানাবেন
YOUTUBE_CHANNEL_ID=UCxxxxxxxxxxxxxxxxxxxx
YOUTUBE_CHECK_INTERVAL_MINUTES=30
```

---

## ধাপ ৫: আপনার ব্যবসার তথ্য সাজানো (`knowledge_base.json`)

প্রজেক্টের `knowledge_base.json` ফাইলটি ওপেন করুন। এখানে আপনার পেজের:
- নাম ও ব্যবসার ধরন
- সার্ভিসের বিবরণ ও মূল্য
- যোগাযোগের ফোন/হোয়াটসঅ্যাপ নম্বর
- সাধারণ প্রশ্নোত্তর (FAQs)
নিজের মতো করে এডিট করে নিন। এআই এই ফাইল দেখেই গ্রাহকদের মেসেজ ও কমেন্টের নির্ভুল উত্তর দেবে।

---

## ধাপ ৬: ২৪/৭ ক্লাউডে চালানো (Railway.app এ ডিপ্লয় - সবচেয়ে সেরা)

আপনার পিসি বা মোবাইল বন্ধ থাকলেও এটি ২৪/৭ ফুল স্পিডে অনলাইনে এক সেকেন্ডও বন্ধ না হয়ে চলার জন্য **[Railway.app](https://railway.app/)** হলো সেরা চয়েস:

1. আপনার কোডটি আপনার ব্যক্তিগত [GitHub](https://github.com/) অ্যাকাউন্টে একটি রিপোজিটরি বানিয়ে পুশ করুন (Private Repo রাখতে পারেন)।
2. ব্রাউজারে যান: [railway.app](https://railway.app/) এবং আপনার GitHub দিয়ে লগইন করুন।
3. ড্যাশবোর্ড থেকে **"New Project"** -> **"Deploy from GitHub repo"** চাপুন এবং আপনার রিপোজিটরিটি সিলেক্ট করুন।
4. ডিপ্লয় শুরু হওয়ার পর প্রজেক্টের **Variables** ট্যাবে যান।
5. আপনার `.env` ফাইলের ডেটাগুলো ভেরিয়েবল হিসেবে যুক্ত করে দিন:
   - `GEMINI_API_KEY`
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_ADMIN_CHAT_ID`
   - `FB_PAGE_ID`
   - `FB_PAGE_ACCESS_TOKEN`
   - `FB_VERIFY_TOKEN`
   - `YOUTUBE_CHANNEL_ID`
6. প্রজেক্টের **Settings** ট্যাবে গিয়ে **"Generate Domain"** বাটনে ক্লিক করুন। আপনি একটি পাবলিক লাইভ ইউআরএল পাবেন (যেমন: `https://fb-ai-automation-production.up.railway.app`)।

*(বিকল্প হিসেবে আপনি চাইলে [Render.com](https://render.com/) এও ডিপ্লয় করতে পারেন, তবে Railway-তে কোনো স্লিপ মোড নেই বলে এটি ২৪/৭ অবিরাম চলে।)*

---

## ধাপ ৭: ফেসবুক ওয়েব-হুক (Webhook) কানেক্ট করা

1. [Meta Developer Portal](https://developers.facebook.com/) এ আপনার অ্যাপে যান।
2. বামপাশের মেনু থেকে **Webhooks**-এ যান।
3. ড্রপডাউন থেকে **"Page"** সিলেক্ট করে **"Subscribe to this object"** চাপুন।
4. **Callback URL:** আপনার Railway বা ক্লাউড লিংকের শেষে `/webhook/facebook` দিন (যেমন: `https://fb-ai-automation-production.up.railway.app/webhook/facebook`)।
5. **Verify Token:** আপনার `.env` ফাইলে দেওয়া `FB_VERIFY_TOKEN` (যেমন: `my_secure_fb_webhook_token_2026`) বসিয়ে ভেরিফাই করুন।
6. ভেরিফাই হলে **Subscription Fields** থেকে নিচের ফিল্ডগুলোতে **Subscribe** বাটনে ক্লিক করুন:
   - `messages` (ইনবক্স মেসেজের জন্য)
   - `feed` (পোস্ট ও কমেন্টের জন্য)

---

## অভিনন্দন! আপনার ২৪/৭ অটোমেশন এখন সম্পূর্ণ প্রস্তুত 🎉

- **পোস্ট ড্রাফট ও অনুমোদন:** ইউটিউব চ্যানেলে নতুন ভিডিও এলেই আপনার টেলিগ্রামে নোটিফিকেশন আসবে। আপনি শুধু মোবাইলে `[✅ Approve & Publish]` চাপবেন।
- **ইনস্ট্যান্ট পোস্ট তৈরি:** যেকোনো ইউটিউব ভিডিওর লিংক আপনার টেলিগ্রাম বটে পাঠিয়ে দিলে এআই সাথে সাথে পোস্ট বানিয়ে দেবে।
- **২৪/৭ গ্রাহক সেবা:** দিনরাত পেজের মেসেঞ্জারে যেকোনো প্রশ্ন এবং পোস্টের কমেন্টে স্বয়ংক্রিয়ভাবে জেমিনি এআই সাবলীল বাংলায় রিপ্লাই দেবে।
